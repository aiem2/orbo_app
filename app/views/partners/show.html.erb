<p style="color: green"><%= notice %></p>
<br>
<h3 class="titulo"><%= @partner.name %></h3>
<%= render @partner %>

<div>
  <% if current_user.admin?%>
    <%= link_to "Editar Partner", edit_partner_path(@partner) %>
    <br>
    <br>
    <%= button_to "Eliminar Partner", @partner, method: :delete, class: "btn", id:"boton" %>
  <% end%>

</div>
