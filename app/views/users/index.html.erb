<h1 class="titulo">Usuarios</h1>

<br>
<h5>Selecciona la información para buscar entre nuestros Usuarios:</h5>
  <%= search_form_for @busqueda do |f| %>
    <%= f.label :name_cont, 'Nombre del Usuario'%>
    <%= f.text_field :name_cont %>

    <%= f.label :id_eq, 'Id'%>
    <%= f.text_field :id_eq %>
    
    <%= f.submit value='Buscar', class: "btn", id: "boton"%>

    <%=link_to 'Nueva búsqueda', users_path, class: "btn", id: "boton" %>
    <%=link_to 'Todos los Partners', users_path, class: "btn", id: "boton" %>
  <% end %>
<br>

<nav class="navbar text-white justify-content-center">
    <%= paginate @users %>
</nav>

<table class="table table-light table-stripped mr-5" id="sortTable">
  <thead>
    <tr>
      <th scope="col">Id</th>
      <th scope="col">Nombre</th>
      <th scope="col">Nick Name</th>
      <th scope="col">Género</th>
      <th scope="col">Mail</th>
      <th scope="col">Teléfono</th>
      <th scope="col">Ciudad</th>
      <th scope="col">Región</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user|%>
    <tr>
        <td><%= user.id %></td>
        <td><%= user.name %></td>
        <td><%= user.nick_name %></td>
        <td><%= user.gender %></td>
        <td><%= user.email %></td>
        <td><%= user.phone %></td>
        <td><%= user.city %></td>
        <td><%= user.state %></td>

    </tr>

    <script>$('#sortTable').DataTable();</script>
    <% end %>
  <tbody>
</table>

<nav class="navbar text-white justify-content-center">
    <%= paginate @users %>
</nav>